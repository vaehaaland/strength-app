# Database URL for Prisma
# For local development (relative to prisma/ directory):
DATABASE_URL="file:./dev.db"

# For Docker/Production (will be set by docker-entrypoint.sh if not specified):
# DATABASE_URL="file:/data/db/app.db"
